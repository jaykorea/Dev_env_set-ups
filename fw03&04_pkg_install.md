# install dependency for freeway_rev03
- install serial pkg
```
sudo apt-get install ros-melodic-serial
```

# install dependency for freeway_rev04
- install py trees
```
sudo apt-get install ros-melodic-py-trees-ros
```
